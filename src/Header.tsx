import { Button } from '@mui/material';

export function Header() {
  return (
    <div>
      <Button variant="contained">Hello world</Button>
      <Button variant="contained">Hello world</Button>
    </div>
  );
}
